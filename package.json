{
  "name": "super-nails",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm --prefix client run dev -- --host\" \"npm --prefix server run dev\"",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
